# ProGuard/R8 rules for the release build.
#
# Minification is currently off (`isMinifyEnabled = false` in app/build.gradle.kts),
# so nothing here is applied yet. The file exists because `proguardFiles` names it
# and the build fails without it. Add keep rules here if minification is turned on.
#
# For more details, see https://developer.android.com/build/shrink-code
